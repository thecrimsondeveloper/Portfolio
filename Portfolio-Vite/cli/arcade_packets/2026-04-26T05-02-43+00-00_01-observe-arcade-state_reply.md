{
  "restated_request": "Create an ArcadeBuilder reply packet for a single quick 'campfire' cozy dodge game (lanterns content) using auto mode/tone; produce the decision record and JSON-ready design details required by the builder for the next steps.",
  "taught_decision_frame": "Prefer existing shared runtime modes and templates; treat one-word flags as seeds; keep game JSON schema-bound; use greybox presentation, no external assets or new JS; produce minimal quick-play loop with clear scoring and fail state.",
  "per_step_decisions": {
    "01-observe-arcade-state": "Assume standard arcade schema and shell templates exist (arcade schema, shell_template). No repo edits; use shared greybox runtime.",
    "02-expand-seed-flags": "Seed -> 'campfire' idea; cozy theme; dodge mechanic; lanterns content; quick depth -> single-scene, short session (30–60s).",
    "03-select-runtime-mode": "Choose shared runtime mode: 'greybox-canvas' (lightweight canvas-based presentation used by portfolio arcade games).",
    "04-plan-game-loop": "Player controls a camper avatar that dodges falling lanterns; survive timer or accumulate survival score; spawn rate ramps; collision ends run.",
    "05-plan-theme-pack": "Warm, muted palette (embers/orange, deep-brown, soft-amber), cozy labels and microcopy, minimal UI — score timer, lives (1), pause.",
    "06-plan-content-contract": "Entities: player, lantern (falling obstacle), ember-particle (VFX lightweight). Systems: spawn, movement, collision, score, difficulty-curve.",
    "07-plan-schema-fields": "Require fields: id, title, runtime, theme (palette/labels), controls, entities[], spawnRules, scoring, failCondition, duration, metadata.",
    "08-fill-json": "Produce minimal game JSON entries (see json_ready_decisions) matching existing arcade schema; no assets referenced.",
    "09-repair-json": "Ensure numeric ranges, required keys present, empty assets lists allowed; use defaults for optional fields (gravity, speed).",
    "10-build-arcade-entry": "Hand off JSON to builder; builder will wrap shell HTML and register entry in portfolio manifest.",
    "11-validate-build": "Validation cues provided (required keys + simple ranges). Recommend builder run schema validator and quick local render check.",
    "12-recommend-next": "Run builder 'build' command and then run a quick browser smoke-test of the shell page."
  },
  "chosen_runtime_mode": "greybox-canvas",
  "root_game_intent": "A short cozy dodge game: sit by the campfire and dodge falling lanterns to keep the campsite safe — quick, warm, reflex-based play suitable for portfolio showcase.",
  "game_loop_plan": {
    "core_action": "Move avatar horizontally to avoid lanterns falling from above.",
    "controls": "Keyboard: ←/→ or A/D; optional touch: drag horizontally.",
    "scoring": "1 point per second survived + 5 points per near-miss (lantern passes within X px).",
    "fail_state": "Collision with any lantern immediately ends run (single-life).",
    "progression": "Spawn interval decreases and fall speed increases every 10s; max difficulty clamp to keep quick depth.",
    "session_length": "Quick depth: default 45 seconds or until collision.",
    "feedback": "Warm particle embers on near-miss, subtle vignette pulse on hit, end-screen summary with score and short cozy message."
  },
  "theme_pack": {
    "palette": {
      "background": "#2B1E16",
      "campfire": "#FFAD33",
      "lantern": "#FFD98A",
      "accent": "#8C5A3C",
      "text": "#F7EFE2"
    },
    "typography": {
      "ui_font": "system-sans",
      "title_style": "soft, medium-weight"
    },
    "mood": "cozy, warm, calming",
    "labels": {
      "title": "Campfire Keep",
      "score": "Warmth",
      "timer": "Nighttime",
      "pause": "Rest",
      "game_over": "Cinders Out"
    },
    "constraints": "No external images; use simple vector/shape lanterns and particles generated by runtime; subdued animations."
  },
  "content_contract": {
    "entities": {
      "player": {
        "id": "player",
        "type": "avatar",
        "hitbox": {"w": 28, "h": 20},
        "speed": 220,
        "appearance": {"shape": "rounded-rect", "colors": ["accent", "campfire"]}
      },
      "lantern": {
        "id": "lantern",
        "type": "obstacle",
        "spawnZone": {"xMin": 0, "xMax": "canvas.width"},
        "sizeRange": {"w": 12, "h": 18},
        "fallSpeedRange": [60, 220],
        "hitboxPadding": 2
      },
      "ember": {
        "id": "ember",
        "type": "vfx",
        "spawnOn": ["nearMiss", "hit"],
        "lifetimeMs": 600
      }
    },
    "systems": {
      "spawnSystem": {"baseIntervalMs": 900, "minIntervalMs": 300, "rampPer10sMs": -80},
      "collisionSystem": {"onCollision": "endRun"},
      "scoreSystem": {"perSecond": 1, "nearMissBonus": 5, "nearMissRadius": 32},
      "uiSystem": {"showScore": true, "showTimer": true, "showPause": true}
    },
    "metadata": {"title": "Campfire Keep", "description": "Dodge falling lanterns in a cozy campfire scene. Quick reflex play."}
  },
  "schema_notes": {
    "map_to_schema": {
      "id": "string (slug)",
      "title": "string",
      "description": "string",
      "runtime": "oneOf existing runtime names (here 'greybox-canvas')",
      "theme": {"palette": "object", "labels": "object"},
      "controls": "object",
      "entities": "array of entity definitions",
      "systems": "array/object of systems with numeric parameters",
      "assets": "array (allowed empty)",
      "duration": "number (seconds, optional for quick depth)",
      "difficultyCurve": "object (spawn rate and speed ramps)"
    },
    "required_fields": ["id","title","runtime","entities","systems","metadata"],
    "defaults": {"assets": [], "controls": {"keyboard": true, "touch": true}}
  },
  "json_ready_decisions": {
    "game_id": "campfire-cozy-lanterns",
    "title": "Campfire Keep",
    "runtime": "greybox-canvas",
    "metadata": {"author": "thecrimsondeveloper", "tags": ["cozy","dodge","quick"]},
    "entities": ["player","lantern","ember"],
    "systems": ["spawnSystem","collisionSystem","scoreSystem","uiSystem"],
    "duration_seconds": 45,
    "assets": []
  },
  "validation_cues": {
    "schema_checks": [
      "Ensure 'id' is slug-safe and unique",
      "Entities array contains definitions for every referenced entity id",
      "'systems' numeric values within sane ranges (speeds 20-1000, intervals >=100ms)",
      "No external URIs in assets list; empty assets allowed",
      "Text labels present for UI (score, timer, game_over)"
    ],
    "smoke_tests": [
      "Builder schema validate -> pass",
      "Shell page loads with canvas element and shows title",
      "Basic control check: left/right move player; lanterns spawn and fall"
    ]
  },
  "downstream_arcade_builder_command_recommendation": "python arcade_builder_cli.py build --id campfire-cozy-lanterns --runtime greybox-canvas",
  "blockers": {
    "none_expected": "If standard arcade schema and shell templates exist, proceed.",
    "possible_blockers": [
      "Missing arcade schema or mismatch in expected runtime name (validator will fail).",
      "Portfolio registration quota or manifest collision for game id.",
      "If builder requires asset placeholders and none are allowed, minimal placeholders must be provided."
    ]
  }
}
