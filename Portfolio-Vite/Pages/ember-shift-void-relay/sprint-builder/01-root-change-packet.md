# Root Change Packet

			## Sprint Objective

			Implement the root slice so the player can reliably feel root under enemy pressure.

			## Player Promise

			The player immediately feels how root improves the route and unlocks Relay the core data to the extraction point before the nebula collapses..

			## Why This Branch Exists

			Root is a distinct, testable slice backed by the canonical brainstorm packet and should land without widening scope.

			## Detail Fractalization

			- Root core slice
  - Implement the root behavior in the main loop.
  - Wire root data into story-structure.json and runtime state.
- Root feedback pass
  - Expose clear HUD and overlay feedback for root.
  - Validate that root stays readable during pressure spikes.

			## Simulated Checkpoints


- Root is driven by canonical brainstorm data rather than ad hoc prompt output.
- Root is visible in live play within the first minute of a run.

			## Acceptance Signals


- The player can describe what root does after one run.
- Root remains readable without breaking the current arena loop.

			## Change Packet Scope


- game.js
- story-structure.json
- index.html

			## Risks And Unknowns


- Root could sprawl beyond the canonical build packet if extra systems are added.
- Root could reduce readability if feedback does not stay compact.

			## Implementation Notes

			- Keep this packet isolated to the current game folder.
			- Use the checkpoints as simulated gates before full implementation.
			- Treat each branch slice as a sub-sprint, not as a merged refactor.
