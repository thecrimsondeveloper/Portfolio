# Validation Loop 02 Goal Spec

- Goal: Make Rooftop While: Guard Break behave like a stealth patrol extraction page instead of a generic relic arena.
- Contract: Preserve `runtimeTemplate = stealth_patrol`, `spatialStructure = patrol grid`, rooftop/tree presentation rules, and the self-contained page folder.
- Success signal: The runtime boots cleanly, exposes patrol pressure in the HUD, and uses uplink capture plus extraction hold as the playable loop.