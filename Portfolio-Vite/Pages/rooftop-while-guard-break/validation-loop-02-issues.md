# Validation Loop 02 Issues

- The prior runtime still behaved like a generic floating relic pickup arena with a late exit beacon, which did not satisfy the `stealth_patrol` plus `patrol grid` design contract.
- Shell copy and arcade metadata still described the older loop and needed to be synchronized with the repaired runtime.